# 日志监控智能体开发计划

> 项目：weekAgent（`E:\weekAgent`）
> 目标：日志文件自动拉取 + LLM 语义分析 + 定时批处理 + 异常告警的独立智能体
> 状态：计划阶段（未实现）
> 日期：2026-07-31

---

## 1. 目标与范围

### 1.1 业务目标

构建一个**日志监控智能体**，实现：

```
定时触发
   ↓
① 日志拉取   —— 从本地日志文件读取（增量、断点续读）
   ↓
② 日志解析   —— 按格式解析时间戳/级别/消息，分块组织
   ↓
③ 语义分析   —— 大模型判断是否异常、异常类型、严重级别、处置建议
   ↓
④ 告警决策   —— 去重、聚合、抑制窗口，判定是否发送
   ↓
⑤ 告警发送   —— 推送到钉钉/企微/邮件/控制台
```

### 1.2 范围界定

| 包含 | 不包含（后续可扩展） |
|---|---|
| 本地文件日志读取（支持 GBK/UTF-8） | 远程日志拉取（SSH/SFTP/云日志服务） |
| 增量拉取 + 断点续读 | 实时流式监听（tail -f） |
| LLM 语义级异常判断 | 规则/阈值引擎（作为可选预筛） |
| 定时批处理（APScheduler / cron） | 可视化 Dashboard |
| 告警推送（webhook / 邮件 / 控制台） | 告警历史统计报表 |

---

## 2. 现状调研结论（2026-07-31 实测）

### 2.1 weekAgent 项目现状

| 项目 | 现状 |
|---|---|
| 框架 | 基于 `hello-agents==1.0.0` 的 FlowUs 周报智能体 |
| Python | venv 为 **Python 3.14.6**（`E:\weekAgent\venv`） |
| LLM 配置 | `.env` 已有 `LLM_MODEL_ID` / `LLM_API_KEY` / `LLM_BASE_URL`（当前指向 bigmodel glm-4.5-Air） |
| 入口 | `run.py`（CLI / Web / Agent 三种模式） |
| 测试样本 | 项目根目录已有 `app.log`（GBK 编码中文日志）可作真实测试数据 |
| 现有结构 | `week_agent/`（agent、flowus、weekly_report、web、cli、config）+ `run.py` + `tests/` |

### 2.2 QwenPaw 2.0.1 复用点调研（本机安装版本）

已确认的可复用组件：

| QwenPaw 组件 | 路径 | 说明 |
|---|---|---|
| Agent 核心 | `agents/react_agent.py` → `QwenPawAgent` | 外部注入 model/toolkit/middlewares，由 `AgentBuilder` 组装 |
| Agent 组装器 | `runtime/builder.py` → `AgentBuilder` | `build_toolkit()` 可挂自定义工具 |
| 定时调度 | `app/crons/`（manager/models/executor） | 基于 **APScheduler** 的 cron 封装（5 字段表达式） |
| 工具守卫 | `governance/` → `PolicyGuardedTool` | 工具调用权限控制（可选） |
| 告警渠道 | `app/channels/`（dingtalk/feishu/slack/wechat） | 现成消息推送通道（与 QwenPaw 应用耦合较重） |

### 2.3 ⚠️ 关键风险：Python 版本不兼容

**QwenPaw 2.0.1 要求 `Requires-Python: >=3.11, <3.14`，而 weekAgent 的 venv 是 Python 3.14.6。**

这意味着：**不能直接把 qwenpaw 装进现有 venv**。

同时 qwenpaw 依赖为"全家桶"（agentscope、playwright、transformers、onnxruntime、各渠道 SDK 等 40+ 包），即使换 Python 版本，也会给周报项目引入大量重依赖，可能污染现有环境。

---

## 3. 技术路线（二选一，需用户确认）

### 路线 A：轻量自研 + 借鉴 QwenPaw 设计（推荐 ✅）

**思路**：不安装 qwenpaw 全家桶，而是借鉴其**核心设计模式**（cron 调度模型、工具注册、agent 循环），用项目现有能力实现。

- 定时：直接用 **APScheduler**（qwenpaw cron 底层就是它），轻量、兼容 Python 3.14
- 分析：复用 `.env` 现有 LLM 配置（不依赖 qwenpaw 的 model 层）
- 告警：自建渠道抽象（webhook/邮件/控制台），后续可换
- 优点：零重依赖、不污染现有 venv、可独立部署、逻辑清晰
- 缺点：不用 qwenpaw 现成代码，需自己写（约 500-800 行）

### 路线 B：新建 Python 3.12 venv + 直接复用 QwenPawAgent

**思路**：为日志智能体单独建一个 Python 3.12 环境，安装 qwenpaw，直接用 `QwenPawAgent` + `AgentBuilder` + 自定义日志工具。

- 需要新建独立 venv（如 `E:\weekAgent\venv_log`），**不用现有项目 venv**
- 需要处理 qwenpaw 重依赖的安装（playwright 需下载浏览器等）
- 与现有周报智能体隔离，互不影响
- 优点：真正复用 QwenPaw 能力（memory、skill 系统等）
- 缺点：环境割裂、依赖重、安装耗时、与项目现有代码风格不一致

### 建议

**推荐路线 A**。理由：
1. 本需求（读本地日志 → LLM 判断 → 定时 → 告警）不需要 QwenPaw 全量能力
2. 避免 Python 3.14 兼容问题和重依赖污染
3. 保持 weekAgent 单一环境、统一入口（`run.py`）
4. 核心设计（cron 模型、工具注册、渠道抽象）完全照抄 QwenPaw 的成熟模式

> 若你坚持"必须直接 import qwenpaw"，则走路线 B，但需要你确认接受独立 venv 方案。

---

## 4. 架构设计（路线 A）

### 4.1 模块划分（新增 `week_agent/log_monitor/`）

```
week_agent/log_monitor/
├── __init__.py
├── config.py          # 配置：日志路径、分析规则、调度、告警渠道
├── fetcher.py         # ① 日志拉取：增量读取、断点续读、编码检测
├── parser.py          # ② 日志解析：格式解析、分块、级别提取
├── analyzer.py        # ③ LLM 语义分析：prompt 构建、结构化输出解析
├── alerter.py         # ④⑤ 告警：去重/聚合/抑制窗口 + 渠道发送
├── scheduler.py       # 定时调度：APScheduler 封装（借鉴 qwenpaw cron 模型）
├── runner.py          # 编排：一次完整执行流程（fetch→parse→analyze→alert）
└── prompts.py         # LLM 分析提示词模板
```

### 4.2 数据流

```
APScheduler 定时触发 (如 */5 分钟)
   ↓
runner.run_once()
   ├─ fetcher.fetch()      读取上次位置之后的新日志（断点存 JSON）
   ├─ parser.parse()       解析为结构化条目 [{ts, level, msg}]，按大小分块
   ├─ analyzer.analyze()   每块调 LLM，输出 {异常?, 级别, 类型, 摘要, 建议}
   ├─ alerter.evaluate()   去重 + 聚合 + 抑制窗口判断
   └─ alerter.send()       发送到配置的渠道（webhook/邮件/控制台）
```

### 4.3 关键设计决策

| 决策点 | 方案 |
|---|---|
| 增量拉取 | 记录每个文件已读字节偏移（`data/log_monitor/offsets.json`），下次从偏移继续 |
| 编码处理 | 自动检测（GBK/UTF-8），当前 `app.log` 为 GBK |
| LLM 成本控制 | 先按级别/关键词粗筛，只对候选块做 LLM 语义分析；块大小限制（如 ≤3000 字符） |
| 告警去重 | 以"异常指纹"（类型+来源行模式）去重，相同指纹在抑制窗口内（如 30min）不重复告警 |
| 告警聚合 | 一批日志的多个异常合并为一条摘要告警（避免告警风暴） |
| 配置方式 | `.env` + `config.py` 默认值，支持 `LOG_MONITOR_*` 环境变量 |
| 与现有项目集成 | 在 `run.py` 增加 `--log-monitor`（单次执行）和 `--log-monitor-daemon`（常驻调度）子命令 |

---

## 5. 分阶段执行计划

### 阶段 0：环境与骨架（约 0.5 天）

**交付物**：模块骨架 + 配置体系

- [ ] 确认技术路线（A/B，需用户拍板）
- [ ] 创建 `week_agent/log_monitor/` 包骨架
- [ ] 编写 `config.py`：日志路径、块大小、LLM 参数、调度表达式、告警渠道配置
- [ ] 在 `.env.example` 增加 `LOG_MONITOR_*` 配置项说明
- [ ] 在 `run.py` 注册 `--log-monitor` / `--log-monitor-daemon` 参数（先打印占位）

**验收**：`python run.py --log-monitor` 能输出"模块就绪"占位信息。

### 阶段 1：日志拉取与解析（约 1 天）

**交付物**：`fetcher.py` + `parser.py`

- [ ] `fetcher.py`：读取单文件/多文件，支持增量偏移、编码自动检测、文件不存在容错
- [ ] `parser.py`：解析常见格式（`时间 | 级别 | 模块 | 消息`，兼容 `app.log` 的 GBK 中文格式），输出结构化条目并分块
- [ ] 断点文件持久化（`data/log_monitor/offsets.json`）
- [ ] 单元测试：用项目根目录 `app.log` 作为测试数据（`tests/test_log_fetcher.py`）

**验收**：`python -m week_agent.log_monitor.fetcher` 能增量读取 `app.log` 并解析出结构化条目。

### 阶段 2：LLM 语义分析（约 1 天）

**交付物**：`analyzer.py` + `prompts.py`

- [ ] 设计分析 prompt：输入日志块，输出 **JSON**（`{is_anomaly, severity, anomaly_type, summary, suggestion}`）
- [ ] `analyzer.py`：调用现有 `.env` 的 LLM（OpenAI 兼容接口），解析 JSON 输出，容错（输出非 JSON 时降级重试）
- [ ] 粗筛规则：可配置关键词/级别白名单，跳过明显正常日志，节省 token
- [ ] 单测：构造含异常/正常日志块，验证分析结果（`tests/test_log_analyzer.py`）

**验收**：对 `app.log` 中一段含 `WARNING`/异常文本的日志，能输出结构化异常判定。

### 阶段 3：告警模块（约 0.5-1 天）

**交付物**：`alerter.py` + 渠道实现

- [ ] 渠道抽象：`BaseChannel` 接口 → `ConsoleChannel`（默认，打印）、`WebhookChannel`（钉钉/企微通用 webhook，POST JSON）、`MailChannel`（可选，复用项目邮件能力）
- [ ] 去重/聚合/抑制窗口逻辑（指纹 + 抑制时间）
- [ ] 告警消息模板（Markdown 格式，含级别、摘要、时间、日志片段）
- [ ] 单测：`tests/test_alerter.py`

**验收**：构造连续两次相同异常，第二次在抑制窗口内不重复发送；控制台能看到格式化告警。

### 阶段 4：定时调度与集成（约 0.5-1 天）

**交付物**：`scheduler.py` + `runner.py` + CLI 集成

- [ ] `runner.py`：编排一次完整执行（fetch→parse→analyze→alert），记录执行日志与统计
- [ ] `scheduler.py`：APScheduler 封装，支持 5 字段 cron 表达式（借鉴 qwenpaw cron 模型：job id / enabled / cron 表达式 / 任务类型）
- [ ] `run.py --log-monitor-daemon`：常驻进程，加载配置启动调度
- [ ] `run.py --log-monitor`：手动执行一次（调试用）
- [ ] 集成测试：修改调度为每分钟执行，观察连续多次运行

**验收**：`--log-monitor-daemon` 常驻运行，按配置表达式定时执行；日志目录新增文件能被增量捕获。

### 阶段 5：测试与文档（约 0.5 天）

**交付物**：完整测试 + 文档

- [ ] `pytest tests/` 全量通过（新增测试 + 回归现有测试）
- [ ] 更新 `README.md`：新增"日志监控智能体"章节（功能、配置、使用、架构）
- [ ] 更新 `.env.example`
- [ ] 代码走查：统一风格、异常处理、日志记录

**验收**：README 按步骤可完成部署与使用；全量测试通过。

---

## 6. 技术要点与风险清单

| # | 风险/要点 | 应对 |
|---|---|---|
| 1 | **Python 3.14 与 qwenpaw 不兼容** | 路线 A 不安装 qwenpaw，规避；若走 B 需独立 venv |
| 2 | LLM 返回非 JSON / 分析不稳定 | prompt 强约束 + 解析容错 + 一次重试 |
| 3 | 日志量大，token 成本高 | 粗筛预过滤 + 分块 + 抑制窗口 |
| 4 | 告警风暴 | 指纹去重 + 聚合摘要 + 抑制窗口 |
| 5 | GBK 中文日志乱码 | 编码自动检测（charset-normalizer / 手动探测） |
| 6 | 日志文件滚动（app.log → app.log.2026-07-21） | fetcher 支持按文件名模式（glob）扫描 + 偏移记录 |
| 7 | 常驻进程崩溃 | daemon 异常捕获 + 断点续读保证不丢日志 |
| 8 | 与现有周报功能冲突 | 新模块独立包，仅扩展 `run.py` 入口，不改现有逻辑 |

---

## 7. 待确认事项

1. **技术路线**：路线 A（轻量自研，推荐）还是 路线 B（独立 venv + 装 qwenpaw）？
2. **告警渠道**：先接哪个？钉钉 / 企业微信 / 邮件 / 仅控制台？（建议先用控制台跑通，再接 webhook）
3. **日志来源**：是否就是 `app.log` 这类格式？还是有其他业务日志（路径/格式）需要支持？
4. **调度频率**：批处理间隔大概多少（如 5 分钟 / 10 分钟）？
5. **分析重点**：主要关心哪类异常？（错误级、WARNING 级、特定业务错误、性能问题？）

---

*本文档为执行计划，经确认后按阶段 0-5 顺序实施。*
