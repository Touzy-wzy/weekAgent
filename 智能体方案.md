# 智能体方案

> 基于 `hello-agents` 框架二次开发，在现有 weekAgent 项目（`E:\weekAgent`）上，面向 **周报生成/发送** 与 **日志监控/告警** 两大场景，沉淀两套可独立运行、也可共享底层能力的子智能体。
>
> - 子智能体 A：**周报智能体**（多轮对话 + 数据拉取 + Excel 填写 + 邮件发送）
> - 子智能体 B：**日志监控智能体**（日志拉取 + LLM 语义分析 + 多渠道告警）
> - 统一入口：`run.py`；统一框架：`hello-agents`（当前 venv），不引入新 Agent 框架。

---

## 一、项目目标

在 Web 端 / CLI 端各提供一个智能体能力，覆盖两类高频自动化需求：

### 1.1 周报智能体

1. **多轮对话 + 反问**收集周报要素（区间、数据源、收件人等）
2. 支持两种数据来源：
   - **数据源 A**：从 FlowUs 工作空间按日期区间自动拉取
   - **数据源 B**：用户上传 Word 文件解析
3. 按固定 Excel 模板（`工作周报-王兆阳-2026.7.24.xlsx`）自动填写生成 `.xlsx` 周报
4. 用户审核周报草稿 → 确认后指定收件人发送邮件（带附件）
5. 全程基于当前虚拟环境的 `hello-agents` 框架二次开发

### 1.2 日志监控智能体

1. 自动拉取本地日志文件（支持 UTF-8 / GBK，多文件）
2. 解析提取 WARNING / ERROR 异常记录（聚合多行续行、规避超长消息）
3. LLM 对异常做**语义级判断**（是否真异常、类型、严重级别、影响面、处置建议）
4. 按需多渠道告警推送（控制台 / 邮件 / 企业微信 webhook）
5. 生成带**位置索引**的 Markdown 报告落盘，并提供行号定位能力

---

## 二、总体架构

```
                          run.py（统一入口）
                 ┌──────────────┴──────────────┐
     ┌─ 周报智能体 ─┐               ┌─ 日志监控智能体 ─┐
     │ AvgWeeklyAgent│               │ LogMonitorAgent │
     │(继承 ReActAgent)│              │(继承 FlowUsAgent)│
     └──────┬──────┘                └──────┬──────┘
            │                              │
week_agent/weekly_report/          week_agent/log_monitor/
  - session（状态机+持久化）          - log_parser（解析，含位置索引）
  - templates / excel_filler       - alerter（多渠道告警）
  - doc_parser                     - agent / tools / run_log_check
  - agent                          -（CLI 入口 run_log_check.py）
            │                              │
            └────────── 共享基础设施 ─────────┘
  hello-agents 框架（ReActAgent / Tool / LLM / 历史压缩 / TraceLogger）
  FlowUs 工具、Agently 邮件工具、工程的 .env LLM 配置
```

| 层 | 说明 |
|---|---|
| 框架层 | `hello-agents`（ReActAgent、Tool+ToolParameter、ToolRegistry、HelloAgentsLLM、HistoryManager、TraceLogger、arun_stream） |
| 公共能力 | FlowUs 拉取、Agently 邮件、LLM 配置、`data/` 落盘、`memory/` 输出 |
| 周报模块 | `week_agent/weekly_report/` + 对应 tools + web 路由 |
| 日志模块 | `week_agent/log_monitor/` + 对应 tools + CLI 入口 |
| 入口层 | `run.py`（`--web` / `--agent` / `--log-agent` / `--log-agent-debug` / `--agent-debug`） |

---

## 三、现有项目能力盘点（可复用）

| 能力 | 现状 | 复用方 |
|---|---|---|
| FlowUs 数据获取 | `flowus_list_pages` / `flowus_get_page` / `flowus_search` | 周报数据源 A 底层调用 |
| FlowUs OAuth + Token 持久化 | [flowus/auth.py](week_agent/flowus/auth.py) | 无需改动 |
| 邮件编写/发送 | `AgentlyComposeMailTool` / `AgentlySendMailTool`（compose→confirm→send 两步确认） | 周报审核发送 + 日志邮件告警 |
| LLM 语义能力 | `.env` 已有 `LLM_MODEL_ID/API_KEY/BASE_URL`；`HelloAgentsLLM` | 周报 LLM 生成 / 日志异常语义判断 |
| FastAPI Web 框架 | [web/app.py](week_agent/web/app.py) | 周报 / 日志相关路由 |
| ReActAgent + 工具注册 | [agent/runner.py](week_agent/agent/runner.py) | 周报编排 Agent 基类 |
| 多轮记忆 Agent | [agent/flowus_agent.py](week_agent/agent/flowus_agent.py)（历史注入 + 智能摘要 + TraceLogger 修复） | 日志监控 Agent 基类 |
| HTTP 客户端 | `httpx` | 企业微信 webhook 推送 |

---

## 4. hello-agents 框架二次开发说明

### 4.1 复用的核心能力

| 框架组件 | 位置 | 用途 |
|---|---|---|
| `ReActAgent` | `agents/react_agent.py` | Agent 基类，Function Calling + Thought/Finish 工具 |
| `FlowUsAgent` | `week_agent/agent/flowus_agent.py` | 带多轮记忆 / 摘要压缩的增强基类 |
| `Tool` + `ToolParameter` | `tools/base.py` | 自定义工具基类，`run()` 返回 `ToolResponse` |
| `ToolRegistry` | `tools/registry.py` | 工具注册表 |
| `HelloAgentsLLM` | `core/llm.py` | LLM 调用，支持 `invoke_with_tools` |
| `HistoryManager` | `context/history.py` | 多轮对话历史（自动压缩 / 智能摘要） |
| `TraceLogger` | `observability/trace_logger.py` | 可观测性日志 |
| `arun_stream()` | `agents/react_agent.py` | 异步流式输出，支持 SSE 转发 |

### 4.2 二次开发要点

- **Agent**：继承 `ReActAgent`（周报）或 `FlowUsAgent`（日志），注入领域 `system_prompt`
- **新工具**：继承 `Tool`，实现 `get_parameters()` 与 `run()`，返回 `ToolResponse`
- **多轮对话**：`HistoryManager` 维护历史；周报另加外层状态机控制流程
- **流式输出**：`arun_stream()` 产出 `StreamEvent`，Web 端 SSE 转发
- **工具内异步**：复用 `_run_async()` 模式（独立线程池跑事件循环），避免与 FastAPI 事件循环冲突
- **不引入新框架**：不使用 LangChain/AutoGen/CrewAI 等

---

# 上篇 · 周报智能体

## 5. 周报会话状态机（对话 + 反问的核心）

### 5.1 状态定义

| 状态 | 含义 | Agent 行为 |
|---|---|---|
| `INIT` | 会话开始 | 反问：周报区间、数据源类型、填报人姓名 |
| `GATHERING` | 收集素材 | 按数据源调 FlowUs 工具或读取已上传 Word |
| `DRAFTING` | 生成结构化数据 | LLM 按模板 schema 输出 JSON |
| `FILLING` | 填写 Excel | openpyxl 生成 xlsx |
| `REVIEWING` | 等待审核 | 暂停，等用户确认 / 修改 |
| `COLLECTING_RECIPIENT` | 收集收件人 | 反问邮箱，多选常用列表 + 手输 |
| `CONFIRMING_SEND` | 发送前确认 | 调 compose_mail 展示摘要，等二次确认 |
| `SENT` / `FAILED` | 终态 | 结束或回退重试 |

### 5.2 反问机制实现

- Agent 在 `INIT` / `COLLECTING_RECIPIENT` 阶段若关键信息缺失，输出特殊标记反问消息（如 `[NEED_INPUT: week_range]`），前端识别渲染为气泡等待输入
- 用户回答后，前端把「反问标记 + 回答」一起回传，状态机推进
- 状态落盘 `data/sessions/{session_id}.json`，重启可恢复
- 与 `HistoryManager` 关系：`HistoryManager` 管 LLM 上下文，外层状态机当「调度器」，Agent 当「执行器」，两者分离

### 5.3 数据流

```
用户在 Web 端发起"生成本周周报"
   ↓ 会话状态机(INIT) → 反问区间/数据源/姓名
   ↓ GATHERING
   ↓   数据源 A：FlowUs 按日期区间搜 → flowus_search/list/get → 素材文本
   ↓   数据源 B：上传 Word → python-docx 解析 → 素材文本
   ↓ DRAFTING：LLM 按模板 schema 生成结构化 JSON
   ↓ FILLING：openpyxl 读模板按坐标写入 → data/drafts/{session_id}.xlsx
   ↓ REVIEWING：渲染 HTML 表格供审核（修改 → 回 DRAFTING）
   ↓ 确认 → COLLECTING_RECIPIENT
   ↓ CONFIRMING_SEND：compose_mail(带附件)→摘要→二次确认
   ↓ send_mail → SENT
```

## 6. 周报模板结构解析

模板：`工作周报-王兆阳-2026.7.24.xlsx`，Sheet1（5 列 A-E，25 行）：

| 区块 | 位置 | 说明 |
|---|---|---|
| 标题/本周 | A1:E1 合并标题；A2:E2 分区「一、本周工作」；Row3 表头(序号/分类/任务/状态/备注)；Row4-8 数据 | 数据验证：分类 `61850/AI/其他`，本周状态 `已完成/进行中/已延期`（B4:B8,D4:D8） |
| 下周计划 | A10:E10「二、下周计划」；Row11 表头(序号/分类/任务/状态/风险)；Row12-16 数据 | 分类同上；下周状态 `完成/开展/延期`（B12:B17,D12:D17） |
| 行业信息 | A18:E18「三、行业信息收集」；A19:E19 填写说明；Row20 表头(序号/信息内容 B-D 合并/备注)；Row21-25 数据 | 信息内容写入合并格 B-D 首格 |

> 三个表格均为最多 5 行，枚举值约束必须遵守，否则破坏数据验证。本项目已实测解析并固化该结论。

## 7. 周报数据源接入

### 7.1 数据源 A：FlowUs 按日期区间拉取

复用现有 3 个 FlowUs 工具，新增组合工具 `flowus_fetch_weekly` 减少推理步数：

| 工具 | 参数 | 行为 |
|---|---|---|
| `flowus_fetch_weekly` | `project_name`, `week_range`（如 `2026.7.22-7.28`） | 内部先 `flowus_search` 按日期+项目找页面，再 `flowus_get_page` 取内容，返回汇总素材文本 |

### 7.2 数据源 B：上传 Word

- 新增 API `POST /api/weekly/upload`（multipart），保存到 `data/uploads/{session_id}/`
- 新增工具 `read_uploaded_doc`：参数 `session_id`，`python-docx` 读取段落+表格返回纯文本
- 前端在 `INIT` 阶段提供上传按钮，上传后绑定 `session_id`

## 8. 周报 Excel 填写（openpyxl）

**依赖**：`openpyxl>=3.1.0`（保留模板样式与数据验证）。

**工具** `fill_weekly_report` 主要参数：`template_path` / `reporter_name` / `week_range` / `this_week_work` / `next_week_plan` / `industry_info`。

**写入逻辑**：
1. `load_workbook(template_path)` 加载模板
2. 按固定坐标写入：本周 `B4:E8`、下周 `B12:E16`、行业信息 `B21:E25`（序号列已预填）
3. 另存为 `data/drafts/工作周报-{name}-{week_range}.xlsx`

**枚举校验**：`category ∈ {61850,AI,其他}`；本周 `status ∈ {已完成,进行中,已延期}`；下周 `status ∈ {完成,开展,延期}`。非法值直接报错不写入。

## 9. 周报邮件发送流程

复用两步确认：
1. 审核通过 → `COLLECTING_RECIPIENT` 收集收件人
2. `agently_compose_mail`：`to` / `subject=工作周报-{name}-{week_range}` / body 摘要 / **附件=xlsx**
3. 返回 `confirmation_token` → 展示摘要 → 二次确认 → `agently_send_mail` → `SENT`

**附件支持**：现有 `AgentlyComposeMailTool` 未传附件参数，需扩展 `agently-cli` 命令：

```bash
agently-cli message +send --to "{to}" --subject "{subject}" --body "{body}" \
  --attachment "{xlsx_filepath}" --confirmation-token "{token}"
```

> 注：需确认 `agently-cli` 是否支持 `--attachment`；若不支持则改为 SMTP 直发或邮件正文下载链接。

**收件人管理**：落盘 `data/recipients.json`，前端多选 + 临时手填。

## 10. 周报 Web API（新增）

| 方法 | 路径 | 作用 |
|---|---|---|
| `POST` | `/api/weekly/sessions` | 创建会话，返回 `session_id` |
| `POST` | `/api/weekly/sessions/{id}/message` | 发送对话，**SSE 流式**返回 |
| `GET` | `/api/weekly/sessions/{id}` | 查询状态 + 草稿元数据 |
| `GET` | `/api/weekly/sessions/{id}/draft` | 下载 xlsx 草稿 |
| `POST` | `/api/weekly/sessions/{id}/upload` | 上传 Word |
| `POST` | `/api/weekly/sessions/{id}/approve` | 审核通过 → 收件人收集 |
| `POST` | `/api/weekly/sessions/{id}/reject` | 要求修改，带意见 |
| `POST` | `/api/weekly/sessions/{id}/send` | 二次确认后真发送 |
| `GET/POST` | `/api/weekly/recipients` | 获取 / 新增常用收件人 |

**SSE**：前端 `fetch` + `ReadableStream` 接收；后端 `agent.arun_stream(query)` 产出 `StreamEvent` → 转为 `event: llm_chunk / tool_call / agent_finish`。

---

# 下篇

## 11. 日志监控智能体（** 已实现 ✅）

### 11.1 模块结构

```
week_agent/log_monitor/
├── __init__.py
├── config.py          # 日志路径、级别、渠道、报告目录（.env 可覆盖 LOG_MONITOR_*）
├── log_parser.py      # 解析：提取异常记录、聚合多行、位置索引(location_manifest)
├── alerter.py         # 多渠道告警：BaseChannel / Console / Mail / Wecom / dispatch
├── tools.py           # LogReadTool（可供 LLM 调用）
├── agent.py           # LogAlertAgent（继承 ReActAgent）+ run_log_check 批处理入口
├── run_log_check.py   # CLI 入口（--preview 仅预览 / 默认完整分析）
└── config 文件（如上）
```

### 11.2 实现状态

| 能力 | 状态 |
|---|---|
| 解析全部监控文件，提取 WARNING/ERROR | ✅ 实测 278 条全链路跑通 |
| 注入真实时间戳 + 报告落盘 `config/REPORT_DIR` | ✅ 报告 `reports/log_alert_*.md` |
| 异常位置索引（LogRecord 增加 start/end_line + location_manifest + 报告索引段） | ✅ |
| 多渠道告警（console / mail / wecom） | ✅ 端到端实测通过 |
| 打包为 FlowUsAgent 形态子智能体（4 工具 + run.py 入口） | ✅ |

### 11.3 核心配置（`log_monitor/config.py`）

| 配置 | 默认 | 说明 |
|---|---|---|
| `LOG_FILES` | `app.log.2026-07-21` / `app.log` | 默认监控文件（可用 `LOG_MONITOR_FILE_*` 覆盖） |
| `WATCH_LEVELS` | `{WARNING, ERROR}` | 只关注异常级 |
| `LOG_ENCODINGS` | `utf-8`, `gbk` | 编码自动候选 |
| `MAX_RECORD_CHARS` | 2000 | 超长截断，防上下文爆炸 |
| `MAX_ANALYZE_RECORDS` | 60 | 单次送 LLM 上限 |
| `ALERT_CHANNELS` | `console,mail,wecom` | 告警渠道集合 |
| `WECOM_MIN_SEVERITY` | `critical` | 企业微信只推达标级别 |
| `MAIL_FREQUENCY` | `daily` | 邮件每日一汇总（去重） |
| `MAIL_TO` | `wangzhaoyang@ieslab.cn` | 邮件收件人 |
| `REPORT_DIR` | `reports/` | 报告落盘目录 |

### 11.4 日志解析（log_parser.py）

- 逐级行解析：识别时间戳 / 级别 / 模块 / 消息正文
- 自动聚合多行续行（traceback 块视为一条，记录首尾行号 `start_line-end_line`）
- `location_manifest(by_file)` 生成位置索引段，追加到报告末尾
- `extract_watch_records(file, max_records)` 供工具读取

### 11.5 多渠道告警（alerter.py）

**统一接口**：`BaseChannel.send(alert)` → 具体渠道，`dispatch_channels(alert)` 批分发，每渠道独立 `try/except` 优雅降级。

| 渠道 | 行为 |
|---|---|
| `ConsoleChannel` | 控制台格式化打印 + 存档 |
| `MailChannel` | 复用 `AgentlySendMailTool`；**每天只发一封汇总**（`reports/.mail_sent/<date>` marker 去重，真正发送成功才标记） |
| `WecomChannel` | 企业微信机器人 webhook；**每条 critical 实时推**（`min_severity` 过滤），markdown 消息 POST |

**统一告警数据结构**（`format_alert_payload`）：`{severity, title, summary, body, time}`。

### 11.6 日志监控 Agent（`LogMonitorAgent`）

继承 `FlowUsAgent`（多轮记忆 / 摘要压缩 / TraceLogger 修复），注册 4 个工具，专注单一职责：

| 工具 | 行为 |
|---|---|
| `log_read_anomalies` | 读取异常记录（时间戳/级别/模块/正文，附带位置索引） |
| `log_position_lookup` | 按关键字/级别/模块/时间戳定位源文件行号区间 |
| `run_log_check` | 完整流程一 步到位：解析 → LLM 分析 → 多渠道推送 → 报告落盘（返回结论+报告路径+推送结果） |
| `log_preview` | 快速统计/预览（**不调 LLM、不告警、不落盘**） |

**统一入口**（`run.py`）：

```
python run.py --log-agent "今天日志怎么样?"          # 单次查询
python run.py --log-agent-debug                      # 交互式调试（多轮记忆生效）
python -m week_agent.log_monitor.run_log_check       # 后台批处理（完整分析+告警）
python -m week_agent.log_monitor.run_log_check --preview    # 仅预览，不调 LLM
```

CLI 设计原则：**先做一次不依赖 LLM 的解析统计**（`parse_all`），保证至少能拿到异常集合，再决定是否进入 LLM 分析。

### 11.7 日志数据流

```
run.py --log-agent / run_log_check 触发
   ↓ ① 解析  parse_all() → 提取 WARNING/ERROR（含位置索引）
   ↓ ② 语义分析 LogAlertAgent(ReAct, LLM, temperature=0.3)：
   ↓     自动读取 → 判断异常类型/严重度/影响/建议 → 输出结构化报告
   ↓ ③ 告警  format_alert_payload(severity,title,summary,body,time)
   ↓ ④ 落盘  save_report → reports/log_alert_<时间戳>.md（附位置索引段）
   ↓ ⑤ 分发  dispatch_channels → console / mail(每日一汇总) / wecom(critical 实时)
```

---

## 12. 新增 / 修改文件清单（合并视角）

```
week_agent/
├── weekly_report/                    # 周报模块（新增）
│   ├── __init__.py / session.py（状态机+持久化）
│   ├── templates.py / excel_filler.py（openpyxl）
│   ├── doc_parser.py（python-docx）/ agent.py（周报 ReActAgent）
├── log_monitor/                      # 日志监控模块（已实现）
│   ├── __init__.py / config.py
│   ├── log_parser.py / alerter.py
│   ├── tools.py / agent.py / run_log_check.py
├── agent/
│   ├── flowus_agent.py               # （日志 Agent 的基类，已存在/增强）
│   ├── log_monitor_agent.py          # 日志监控子智能体（含 4 工具 + SYSTEM_PROMPT）
│   ├── runner.py
│   └── tools/
│       ├── flowus_weekly_tool.py / read_uploaded_doc_tool.py / fill_excel_tool.py
│       ├── log_monitor_tools.py      # 日志 4 工具
│       └── agently_mail_tools.py     # 扩展 --attachment
├── web/app.py / services.py          # 周报路由 + 服务
data/                                # 会话 / 上传 / 草稿（周报）；sessions 复用
reports/                              # 日志监控报告落盘（log_alert_*.md，含 .mail_sent marker）
requirements.txt                      # openpyxl、python-docx、python-multipart、httpx
static/index.html                     # 周报助手 Tab（可选日志视图）
run.py                                # 统一入口（已支持 --log-agent / --log-check 等）
```

## 13. 新增依赖

```
openpyxl>=3.1.0          # 周报 xlsx 读写
python-docx>=1.1.0       # 周报上传 Word 解析
python-multipart>=0.0.6  # FastAPI 上传
httpx                     # 企业微信 webhook（日志告警）
```

---

## 14. 两个智能体的关系与复用

| 维度 | 周报智能体 | 日志监控智能体 |
|---|---|---|
| 基类 | `ReActAgent` | `FlowUsAgent`（增强记忆） |
| 触发 | Web 会话状态机（多轮反问） | 命令行 / 单次查询 / （可扩展定时） |
| 数据源 | FlowUs / 上传 Word | 本地日志文件 |
| 产出 | `.xlsx` 周报 → 邮件发送 | Markdown 报告 + 多渠道告警 |
| 邮件 | 按周报组合发到指定收件人（附件） | 每日一汇总（去重） |
| 对话 | 多轮状态机 | 多轮记忆（预 debug 已验证） |

两者共享：`hello-agents` 框架、`.env` 的 LLM、`data/` 落盘、`reports/` 输出、Control 能力。各自独立模块，不互相侵入，仅通过 `run.py` 统一入口暴露。

## 15. 关键风险与对策（合并）

| 风险 | 对策 |
|---|---|
| LLM 不按模板输出 / 分析不稳定 | 周报 schema 强约束 + 校验重试；日志低 token 强约束 + 解析容错 |
| 长文本撑爆上下文 | 周报取摘要；日志 `MAX_RECORD_CHARS` 截断 + `MAX_ANALYZE_RECORDS` 上限 |
| Excel 枚举写错破坏数据验证 | 写前校验，非法值报错 |
| 邮件误发 / 重复发 | 两步确认；日志邮件 daily 去重，真发成功才标 marker |
| 告警风暴 | 企业微信仅 critical 实时推；邮件日汇总；级别过滤 |
| 日志文件滚动 / 编码 | glob 扫描 + 多编码候选（utf-8/gbk）；多行聚合记录位置 |
| LLM 配置缺失 | `check_llm_config()` 启动检测，缺失即报错禁用 LLM 分析 |
| 渠道单点失败 | `dispatch_channels` 每渠道独立 try/except 批量优雅降级 |
| agently-cli 不支持附件 / 未授权 | 备选 SMTP / 链接；启动检测禁用发送 |

## 16. 决策记录

| 决策项 | 选择 |
|---|---|
| 开发框架 | hello-agents（当前 venv），不引入新框架 |
| 周报模板 | 实际 xlsx 模板（3 表格 + 枚举约束） |
| 周报输出 | `.xlsx`（基于模板填写） |
| 周报收件人 | 常用列表 `data/recipients.json` + 临时手填 |
| 周报数据源 | 日期区间搜 FlowUs（可上传 Word） |
| 周报对话 | SSE 流式 + 外层状态机 |
| 日志架构 | 复用 FlowUsAgent + 面向工具 + `config.py` 可配置 |
| 日志监控级别 | 仅 WARNING/ERROR；`MAX_*` 控成本 |
| 日志告警渠道 | console + 邮件（每日一汇总）+ 企业微信（critical 实时） |
| 日志报告 | Markdown 落盘 `reports/`，含位置索引 |
| 告警去重 | 日志：daily 邮件去重 + 企业微信 critical 过滤 |
| 入口 | `run.py` 统一（--web / --agent / --log-agent / --log-check） |

---

## 17. 后续可扩展

- **日志定时调度**：接入 APScheduler（`--log-monitor-daemon`），按 cron 批处理（当前为单次批处理）
- **日志远程拉取**：SSH/SFTP / 云日志服务（当前为本地文件）
- **告警历史统计 / Dashboard**：基于 `reports/` 沉淀做可视化
- **周报自动化**：接入企业微信/钉钉渠道的发送（复用告警渠道抽象）
- **规则预筛**：告警前按关键字/指纹去重聚合，进一步降成本、抑风暴