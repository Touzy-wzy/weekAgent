# 周报智能体方案文档

> 基于 hello-agents 框架二次开发，在现有 weekAgent 项目上扩展周报自动填写与邮件发送能力。

---

## 一、项目目标

在 Web 端实现一个**周报生成与发送智能体**：

1. 通过**多轮对话 + 反问**收集周报要素（区间、数据源、收件人等）
2. 支持两种数据来源：
   - **数据源 A**：从 FlowUs 工作空间按日期区间自动拉取
   - **数据源 B**：用户上传 Word 文件解析
3. 按固定 Excel 模板（`工作周报-王兆阳-2026.7.24.xlsx`）自动填写生成 `.xlsx` 周报
4. 用户审核周报草稿（可修改）→ 确认后指定收件人发送邮件（带附件）
5. 全程基于当前虚拟环境的 `hello-agents` 框架二次开发，不引入新 Agent 框架

---

## 二、现有项目能力盘点（可直接复用）

| 能力 | 现状 | 复用方式 |
|---|---|---|
| FlowUs 数据获取 | 3 个工具：`flowus_list_pages` / `flowus_get_page` / `flowus_search` | 周报数据源 A 的底层调用 |
| FlowUs OAuth + Token 持久化 | [flowus/auth.py](file:///e:/weekAgent/week_agent/flowus/auth.py) | 无需改动 |
| 邮件编写/发送 | `AgentlyComposeMailTool` + `AgentlySendMailTool`（已带 confirmation_token 两步确认机制） | 审核后发送邮件 |
| FastAPI Web 框架 | [web/app.py](file:///e:/weekAgent/week_agent/web/app.py) | 新增周报相关路由 |
| ReActAgent + 工具注册 | [agent/runner.py](file:///e:/weekAgent/week_agent/agent/runner.py) | 周报编排 Agent 的基类 |

---

## 三、hello-agents 框架二次开发说明

通过解析 `venv/Lib/site-packages/hello_agents/`，框架已提供以下能力，本方案在此基础上扩展：

### 3.1 复用的核心能力

| 框架组件 | 位置 | 用途 |
|---|---|---|
| `ReActAgent` | `agents/react_agent.py` | 周报 Agent 基类，基于 Function Calling，内置 Thought/Finish 工具 |
| `Tool` + `ToolParameter` | `tools/base.py` | 自定义工具基类，`run()` 返回 `ToolResponse` |
| `ToolRegistry` | `tools/registry.py` | 工具注册表 |
| `HelloAgentsLLM` | `core/llm.py` | LLM 调用，支持 `invoke_with_tools` |
| `HistoryManager` | `context/history.py` | 多轮对话历史管理（自动压缩） |
| `TraceLogger` | `observability/trace_logger.py` | 可观测性日志 |
| `arun_stream()` | `agents/react_agent.py` | 异步流式输出，支持 SSE 转发 |

### 3.2 二次开发要点

- **周报 Agent**：继承 `ReActAgent`，在 `system_prompt` 中注入周报填写规则与状态机约束
- **新工具**：继承 `Tool`，实现 `get_parameters()` 与 `run()`，返回 `ToolResponse`
- **多轮对话**：利用 `HistoryManager` 维护对话历史，外层加状态机控制流程
- **流式输出**：调用 `arun_stream()` 产出 `StreamEvent`，Web 端用 SSE 转发
- **工具内异步**：复用现有 `_run_async()` 模式（独立线程池跑事件循环），避免与 FastAPI 事件循环冲突

### 3.3 不引入新框架

不使用 LangChain/AutoGen/CrewAI 等，所有 Agent 能力均来自 `hello-agents`。

---

## 四、周报模板结构解析

模板文件：`工作周报-王兆阳-2026.7.24.xlsx`

### 4.1 工作表布局（Sheet1，5 列 A-E，25 行）

```
A1:E1  (合并)  工作周报                         ← 标题
A2:E2  (合并)  一、本周工作                     ← 分区标题
Row3:  序号 | 分类 | 任务描述 | 状态 | 备注/问题   ← 表头
Row4:  1   | AI    | 学习智能体...  | 进行中 | 
Row5:  2   | 其他   | 新员工培训...  | 已完成 | 
Row6-8: 3,4,5 (空行待填)
Row9:  (空，B9 单独有数据验证)

A10:E10 (合并) 二、下周计划
Row11: 序号 | 分类 | 任务描述 | 状态 | 风险       ← 表头（注意 E 列是"风险"）
Row12: 1   | AI    | 智能体案例... | 开展   | 
Row13-16: 2,3,4,5 (空行待填)
Row17: (空)

A18:E18 (合并) 三、行业信息收集
A19:E19 (合并) 填写说明：收集新技术、产品...     ← 说明文字
Row20: 序号 | 信息内容(B20:D20 合并)    | 备注    ← 表头
Row21-25: 1-5 (空行待填，B-D 合并)
```

### 4.2 数据验证（下拉列表枚举）

| 单元格范围 | 允许值 |
|---|---|
| `B4:B8`, `B12:B16`（分类列） | `61850` / `AI` / `其他` |
| `D4:D8`（本周状态） | `已完成` / `进行中` / `已延期` |
| `D12:D17`（下周状态） | `完成` / `开展` / `延期` |
| `B9`（特殊单格） | `61850` / `AI` |

### 4.3 模板固化结论

周报由 **3 个表格**组成：
1. **本周工作表**：5 行，列 = [序号, 分类, 任务描述, 状态, 备注/问题]
2. **下周计划表**：5 行，列 = [序号, 分类, 任务描述, 状态, 风险]
3. **行业信息收集表**：5 行，列 = [序号, 信息内容(合并格), 备注]

填写时必须遵守枚举值约束，否则破坏数据验证。

---

## 五、整体架构与数据流

```
用户在 Web 端发起"生成本周周报"
        ↓
周报会话状态机 (INIT)
        ↓ 反问缺失信息（区间、数据源、姓名）
        ↓
GATHERING 阶段：
  数据源 A：FlowUs 按日期区间搜 → flowus_search/list/get 工具 → 素材文本
  数据源 B：上传 Word → python-docx 解析 → 素材文本
        ↓
DRAFTING 阶段：
  LLM 按周报模板字段 schema 生成结构化数据（JSON）
        ↓
FILLING 阶段：
  openpyxl 读取模板 → 按单元格坐标写入 → 保存为 data/drafts/{session_id}.xlsx
        ↓
REVIEWING 阶段：
  展示 xlsx 草稿（渲染为 HTML 表格）给用户审核
        ↓
  用户确认 → COLLECTING_RECIPIENT（收集收件人）
  用户修改 → 回到 DRAFTING（带修改意见）
        ↓
CONFIRMING_SEND：
  agently_compose_mail（带 xlsx 附件）→ 展示发送摘要
        ↓
  用户二次确认 → agently_send_mail → SENT
```

---

## 六、会话状态机（对话+反问的核心）

### 6.1 状态定义

| 状态 | 含义 | Agent 行为 |
|---|---|---|
| `INIT` | 会话开始 | 反问关键参数：周报区间、数据源类型、填报人姓名 |
| `GATHERING` | 收集素材 | 根据数据源调 FlowUs 工具或读取已上传 Word |
| `DRAFTING` | 生成结构化数据 | LLM 按模板 schema 输出 JSON，转 `FILLING` |
| `FILLING` | 填写 Excel | 调 openpyxl 工具生成 xlsx，转 `REVIEWING` |
| `REVIEWING` | 等待审核 | 暂停，等用户确认/修改 |
| `COLLECTING_RECIPIENT` | 收集收件人 | 反问收件人邮箱，可多选常用列表 + 手输 |
| `CONFIRMING_SEND` | 发送前确认 | 调 compose_mail，展示摘要，等用户二次确认 |
| `SENT` / `FAILED` | 终态 | 结束或回退重试 |

### 6.2 反问机制实现

- Agent 在 `INIT` / `COLLECTING_RECIPIENT` 阶段，若关键信息缺失，输出特殊标记的"反问消息"（如 `[NEED_INPUT: week_range]`），前端识别后渲染成对话气泡等待用户输入
- 用户回答后，前端把"反问标记 + 用户回答"一起回传，状态机推进
- 会话状态用内存字典 + JSON 落盘（`data/sessions/{session_id}.json`），重启可恢复

### 6.3 与 hello-agents HistoryManager 的关系

- `HistoryManager` 维护对话历史（LLM 上下文）
- 外层状态机控制流程推进（决定何时调哪个工具）
- 两者分离：状态机是"调度器"，Agent 是"执行器"

---

## 七、两种数据源接入

### 7.1 数据源 A：FlowUs 按日期区间拉取

复用现有 3 个 FlowUs 工具，新增一个组合工具 `flowus_fetch_weekly` 减少推理步数：

| 工具名 | 参数 | 行为 |
|---|---|---|
| `flowus_fetch_weekly` | `project_name`, `week_range`（如 `2026.7.22-7.28`） | 内部先 `flowus_search` 按日期+项目找周报相关页面，再 `flowus_get_page` 取内容，返回汇总素材文本 |

### 7.2 数据源 B：上传 Word 文件

- 新增 API `POST /api/weekly/upload`（multipart），保存到 `data/uploads/{session_id}/`
- 新增工具 `read_uploaded_doc`：参数 `session_id`，用 `python-docx` 读取并返回纯文本（段落 + 表格）
- 前端在 `INIT` 阶段提供"上传 Word"按钮，上传后把 `session_id` 绑定到会话

---

## 八、Excel 填写实现

### 8.1 依赖

新增 `openpyxl>=3.1.0`（读取/写入 xlsx，保留模板格式与数据验证）。

### 8.2 填写工具 `fill_weekly_report`

| 参数 | 类型 | 说明 |
|---|---|---|
| `template_path` | string | 模板 xlsx 路径（默认 `工作周报-王兆阳-2026.7.24.xlsx`） |
| `reporter_name` | string | 填报人姓名（用于文件名） |
| `week_range` | string | 周报区间（用于文件名） |
| `this_week_work` | array | 本周工作条目列表，每项 = `{category, task, status, remark}` |
| `next_week_plan` | array | 下周计划条目列表，每项 = `{category, task, status, risk}` |
| `industry_info` | array | 行业信息列表，每项 = `{content, remark}` |

### 8.3 写入逻辑

1. `openpyxl.load_workbook(template_path)` 加载模板（保留样式与数据验证）
2. 按固定坐标写入：
   - 本周工作：`B4:E8`（最多 5 行，分类/状态必须用枚举值）
   - 下周计划：`B12:E16`（最多 5 行）
   - 行业信息：`B21:E25`（信息内容写入合并格 B-D 的首格）
3. 序号列 A 已预填 1-5，无需写
4. 另存为 `data/drafts/工作周报-{reporter_name}-{week_range}.xlsx`

### 8.4 枚举值校验

写入前校验：
- `category` ∈ {61850, AI, 其他}
- 本周 `status` ∈ {已完成, 进行中, 已延期}
- 下周 `status` ∈ {完成, 开展, 延期}

非法值直接报错，不写入。

---

## 九、邮件发送流程

### 9.1 复用现有两步确认机制

1. **草稿审核通过** → 状态转 `COLLECTING_RECIPIENT`
2. **收集收件人** → 反问或前端选择常用收件人
3. **调 `agently_compose_mail`**：
   - `to` = 收件人列表（逗号分隔）
   - `subject` = `工作周报-{name}-{week_range}`
   - `body` = 周报摘要文本（本周工作要点 + 下周计划要点）
   - **附件** = 生成的 xlsx 文件路径
   - 返回 `confirmation_token`
4. **展示发送摘要**（收件人/主题/附件名）→ 等用户二次确认
5. **用户确认** → 调 `agently_send_mail`（带 `confirmation_token`）→ 真正发送
6. **发送结果**回写会话状态（`SENT` / `FAILED`）

### 9.2 附件支持

现有 `AgentlyComposeMailTool` 未传附件参数，需扩展 `agently-cli` 调用命令：

```bash
agently-cli message +send \
  --to "{to}" \
  --subject "{subject}" \
  --body "{body}" \
  --attachment "{xlsx_filepath}" \
  --confirmation-token "{token}"
```

> 注：需确认 `agently-cli` 是否支持 `--attachment`，若不支持则改为先把 xlsx 放到邮件正文链接或改用 SMTP 直发。

### 9.3 收件人管理

- 落盘 `data/recipients.json` 存常用收件人列表
- 结构：`[{"name": "张三", "email": "zhangsan@example.com"}, ...]`
- 前端多选 + 临时手输新邮箱

---

## 十、Web API 设计（新增）

| 方法 | 路径 | 作用 |
|---|---|---|
| `POST` | `/api/weekly/sessions` | 创建周报会话，返回 `session_id` |
| `POST` | `/api/weekly/sessions/{id}/message` | 发送对话（反问/指令），**SSE 流式**返回 Agent 输出 |
| `GET` | `/api/weekly/sessions/{id}` | 查询会话状态 + 当前草稿元数据 |
| `GET` | `/api/weekly/sessions/{id}/draft` | 下载当前 xlsx 草稿 |
| `POST` | `/api/weekly/sessions/{id}/upload` | 上传 Word 文件绑定到会话 |
| `POST` | `/api/weekly/sessions/{id}/approve` | 审核通过，进入收件人收集 |
| `POST` | `/api/weekly/sessions/{id}/reject` | 要求修改，带修改意见 |
| `POST` | `/api/weekly/sessions/{id}/send` | 二次确认后真正发送邮件 |
| `GET` | `/api/weekly/recipients` | 获取常用收件人列表 |
| `POST` | `/api/weekly/recipients` | 新增常用收件人 |

### 10.1 SSE 流式实现

对话端点 `POST /api/weekly/sessions/{id}/message`：

- 前端用 `EventSource` 或 `fetch` + `ReadableStream` 接收
- 后端调用 `agent.arun_stream(query)` 产出 `StreamEvent`
- 把 `StreamEvent` 转为 SSE 格式推送：
  ```
  event: llm_chunk
  data: {"chunk": "本周工作..."}

  event: tool_call
  data: {"tool": "flowus_fetch_weekly", "args": {...}}

  event: agent_finish
  data: {"result": "..."}
  ```

---

## 十一、新增/修改文件清单

```
week_agent/
├── weekly_report/                        # 新增模块
│   ├── __init__.py
│   ├── session.py                        # 会话状态机 + JSON 持久化
│   ├── templates.py                      # 周报模板字段 schema + 枚举常量
│   ├── excel_filler.py                   # openpyxl 填写 xlsx
│   ├── doc_parser.py                     # python-docx 解析上传 Word
│   └── agent.py                          # 周报专用 Agent（继承 ReActAgent）
├── agent/
│   ├── tools/
│   │   ├── flowus_weekly_tool.py         # 新增：flowus_fetch_weekly 组合工具
│   │   ├── read_uploaded_doc_tool.py     # 新增：读取上传 Word
│   │   ├── fill_excel_tool.py            # 新增：填写 xlsx 工具
│   │   └── agently_mail_tools.py         # 修改：支持 --attachment 参数
│   └── prompts.py                        # 新增 WEEKLY_REPORT_PROMPT
├── web/
│   ├── app.py                            # 新增周报相关路由
│   └── services.py                       # 新增 weekly 相关服务函数
data/
├── sessions/                             # 会话状态 JSON
├── uploads/                              # 上传的 Word 文件
├── drafts/                               # 周报 xlsx 草稿
└── recipients.json                       # 常用收件人
requirements.txt                          # 新增 openpyxl, python-docx, python-multipart
static/index.html                          # 新增"周报助手"Tab
工作周报-王兆阳-2026.7.24.xlsx             # 模板（已存在）
```

---

## 十二、新增依赖

```
openpyxl>=3.1.0          # xlsx 读写
python-docx>=1.1.0       # Word 解析
python-multipart>=0.0.6  # FastAPI 文件上传
```

---

## 十三、前端交互

在现有 `static/index.html` 新增"周报助手"视图：

- **左侧对话区**：气泡式，区分用户/Agent/系统提示，支持反问
- **右侧草稿预览区**：渲染 xlsx 为 HTML 表格，展示当前草稿
- **操作按钮**：上传 Word / 确认审核 / 要求修改 / 选择收件人 / 发送
- **收件人选择器**：多选常用列表 + 临时手输

---

## 十四、关键风险与对策

| 风险 | 对策 |
|---|---|
| LLM 不按模板字段输出 JSON | 系统提示词强约束 + 生成后用 schema 校验，失败重试 |
| FlowUs 拉取内容过长撑爆上下文 | 复用现有 4000 字符截断 + 周报素材只取摘要 |
| Excel 枚举值写错破坏数据验证 | 写入前校验，非法值直接报错 |
| 邮件误发 | 强制两步确认（compose → 用户确认 → send），`SENT` 前任何一步可取消 |
| 多轮会话状态丢失 | 每次状态变更落盘 JSON，重启可恢复 |
| Word 解析格式多样 | python-docx 提取段落+表格纯文本，复杂格式提示用户校对 |
| agently-cli 不支持附件 | 备选方案：SMTP 直发（`smtplib`）或邮件正文附下载链接 |
| agently-cli 未安装/未授权 | 启动时检测，缺失则禁用发送功能并提示用户 |

---

## 十五、实现优先级（建议分阶段）

1. **阶段一**：周报会话状态机 + Excel 填写工具（最小可用，纯本地生成 xlsx）
2. **阶段二**：FlowUs 数据源接入 + LLM 生成结构化数据
3. **阶段三**：Word 上传解析
4. **阶段四**：邮件发送（含附件）+ 收件人管理
5. **阶段五**：SSE 流式 + 前端周报助手视图

---

## 十六、决策记录

| 决策项 | 选择 | 说明 |
|---|---|---|
| 周报模板 | 实际 xlsx 模板（已解析） | 3 个表格 + 枚举值约束 |
| 输出格式 | `.xlsx`（基于模板填写） | 保留模板样式与数据验证 |
| 收件人管理 | 常用列表（recipients.json）+ 临时手输 | 落盘可复用 |
| FlowUs 数据范围 | 按日期区间搜 | 如 `2026.7.22-7.28` + 项目名 |
| 对话端点 | SSE 流式 | 复用 `arun_stream()` |
| 开发框架 | hello-agents（当前 venv） | 不引入新框架 |
